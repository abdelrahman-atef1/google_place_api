name: google_place_api
description: Google Places API for Flutter — search, details, photos, autocomplete, and proxy-ready HTTP transport.
version: 1.1.2
homepage: https://github.com/abdelrahman-atef1/google_place_api

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.0.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
